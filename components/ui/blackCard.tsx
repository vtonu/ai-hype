import * as React from 'react';
import { Card } from '@/components/ui/card';

export function BlackCard() {
  return (
    <Card className="h-[100px] flex flex-col items-center justify-center p-2 font-sans">
      black card
    </Card>
  );
}
